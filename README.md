Simple bot discord qui permet de configurer un message qui s'enverra à chaque personne qui envoie un message privé au bot.

Commandes: 
!set : permet de modifier le message envoyez lorsque le bot reçoit un message privé.
!status : permet d'afficher le message enregistré
